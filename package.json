{
  "name": "sidescroller_pixi",
  "version": "1.0.0",
  "description": "Scroller game",
  "main": "index.html",
  "scripts": {
    "start": "npm run open",
    "open": "concurrently \"http-server -a localhost -p 1234\" \"open http://localhost:1234/\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.0.2",
    "http-server": "^0.12.0"
  },
  "dependencies": {
    "open": "^7.0.0"
  }
}
